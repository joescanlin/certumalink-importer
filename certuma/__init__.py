"""certuma — the Certuma Reach application (orchestrator, DB, gate, dashboard).

Depends on certuma_core (pure logic). This package is NOT stdlib-only: it uses
SQLAlchemy/Alembic. The dependency arrow points one way: certuma -> certuma_core.
"""
